<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Scoped to the crash-report directory only. A FileProvider grant must never expose
         the whole files/ root (TLS keystore, SFTP known-hosts, DataStore live there). -->
    <files-path name="diagnostics" path="diagnostics/" />
</paths>
